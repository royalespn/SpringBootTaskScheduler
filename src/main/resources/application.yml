
spring:
  application:
    name: spring-boot-task-scheduler

# PushGateway
management:
  metrics:
    export:
      prometheus:
        pushgateway:
          enabled: true
          base-url: http://localhost:9091
          shutdown-operation: push
  endpoints:
    web:
      exposure:
        include: prometheus